<div class="main">
  {{#if hasBulletins }}
    {{#each bulletin in controller}}
      <div class="bulletin-wrapper">
        <div class="vote">
          {{#unless hasBeenVotedOn}}<button {{action voteOnBulletin bulletin}}>&uarr;</button>{{/unless}}
        </div>
        {{#if bulletin.isPost}}
          <div class="bulletin"  {{action goToBulletin bulletin}}>
            <div class="preview">
              <span class="title"> {{bulletin.title}}: </span>
              <span class="preview"> {{bulletin.preview}}</span>
            </div>
          </div>
        {{else}}
          <a class="bulletin" {{bindAttr href="bulletin.url"}}>
            <div class="preview">
              <span class="title">{{bulletin.title}}</span>
            </div>
          </a>
        {{/if}}
      </div>
    {{/each}}
  {{else}}
    No bulletins have been written. Want to {{#linkTo "bulletins.new"}}write one?{{/linkTo}}
  {{/if}}
</div>
<aside>
  <h4> About</h4>
  <p>
    Bulletins are the easiest way to spread the word among the CollegeDesis community.
  </p>
  <p>Post a link or write a blog post. If enough people vote it up, we'll automatically <a href="//twitter.com/collegedesis">tweet</a> it out.</p>
  {{#if controllers.application.currentUser}}
    <button {{action write}}>Post</button>
  {{else}}
    {{#linkTo login}}Log in{{/linkTo}} to post.
  {{/if}}
</aside>