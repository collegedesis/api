<h1 {{action goHome}}><span>College</span>Desis</h1>
<div class="subtitle">Connecting all the South Asian student organizations in America</div>
<nav>
  <ul>
    {{#unless currentUser}}
    <li>{{#linkTo 'index'}}Home{{/linkTo}}</li>
    {{/unless}}
    <li>{{#linkTo bulletins.index}}News{{/linkTo}}</li>
    <li>{{#linkTo radio}}Radio{{/linkTo}}</li>
    <li>{{#linkTo about}}About{{/linkTo}}</li>
  </ul>
  <div class="account">
    {{#if currentUser}}
      {{#linkTo 'users.show' currentUser }}<img {{bindAttr src="currentUser.avatar_url"}} />Profile{{/linkTo}}
    {{/if}}
    {{#if currentUser}}<a {{action logout}}>Sign out</a></li>{{/if}}
  </div>
</nav>