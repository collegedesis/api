<h3>{{name}}</h3>
{{#if university_name}}<h4 class="small muted">{{university_name}}</h4>{{/if}}

{{partial 'd/links'}}

<table class="left-heading">
  <tr>
    <td>Reputation</td>
    <td>{{reputation}}</td>
  </tr>
  <tr>
    <td>Members</td>
    <td>{{memberships.length}}</td>
  </tr>
  {{#if currentUser}}
    <tr>
      <td>You</td>
      <td>{{currentUserStatus}}</td>
    </tr>
  {{/if}}
</table>